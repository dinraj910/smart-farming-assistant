# Copy this file to .env and fill in real values before running.
# main.py doesn't read these yet (Model 1 needs no DB) — this is here
# so the DB connection is one less thing to figure out when you wire up Postgres.
DATABASE_URL=postgresql://user:password@host:5432/dbname
